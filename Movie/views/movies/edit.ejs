<!DOCTYPE html>
<html>
<head>
    <title>Edit Movie</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center p-6">

    <div class="bg-gray-800 p-8 rounded-2xl shadow-lg w-full max-w-4xl">
        <h1 class="text-3xl font-bold mb-6 text-center text-yellow-400">✏ Edit Movie</h1>

        <% if (error) { %>
            <p class="text-red-400 mb-4"><%= error %></p>
        <% } %>

        <form action="/movies/edit/<%= movie._id %>" method="POST" enctype="multipart/form-data" class="space-y-4">
            
            <div class="grid grid-cols-2 gap-6">
                <div class="flex items-center gap-4">
                    <label for="title" class="w-32 text-right">Title:</label>
                    <input type="text" id="title" name="title" value="<%= movie.title %>" required class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400">
                </div>

                <div class="flex items-center gap-4">
                    <label for="director" class="w-32 text-right">Director:</label>
                    <input type="text" id="director" name="director" value="<%= movie.director %>" required class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400">
                </div>

                <div class="flex items-center gap-4 col-span-2">
                    <label for="description" class="w-32 text-right">Description:</label>
                    <textarea id="description" name="description" required class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400"><%= movie.description %></textarea>
                </div>

                <div class="flex items-center gap-4">
                    <label for="rating" class="w-32 text-right">Rating:</label>
                    <input type="number" id="rating" name="rating" value="<%= movie.Rating %>" min="0" max="10" step="0.1" required class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400">
                </div>

                <div class="flex items-center gap-4">
                    <label for="releaseDate" class="w-32 text-right">Release Date:</label>
                    <input type="date" id="releaseDate" name="releaseDate" value="<%= movie.releaseDate.toISOString().split('T')[0] %>" required class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400">
                </div>

                <div class="flex items-center gap-4 col-span-2">
                    <label for="posterImage" class="w-32 text-right">Poster Image:</label>
                    <input type="file" id="posterImage" name="posterImage" accept="image/*" class="flex-1 p-2 rounded bg-gray-700 border border-gray-600 focus:outline-none focus:border-yellow-400">
                </div>
            </div>

            <div class="flex justify-between mt-6">
                <a href="/movies" class="px-6 py-2 bg-gray-600 hover:bg-gray-500 rounded-lg text-white">⬅ Back</a>
                <button type="submit" class="px-6 py-2 bg-yellow-500 hover:bg-yellow-400 text-black font-semibold rounded-lg">Save Changes</button>
            </div>
        </form>
    </div>

</body>
</html>
